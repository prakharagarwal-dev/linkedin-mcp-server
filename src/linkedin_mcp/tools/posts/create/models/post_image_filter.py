from __future__ import annotations

from enum import StrEnum


class PostImageFilter(StrEnum):
    ORIGINAL = "original"
    STUDIO = "studio"
    SPOTLIGHT = "spotlight"
    PRIME = "prime"
    CLASSIC = "classic"
    EDGE = "edge"
    LUMINATE = "luminate"
